version: '3.8'
services:
  postgres:
    image: postgres:16.2
    restart: unless-stopped
    env_file:
      - env/postgres/.env.local
    volumes:
      - ./mount_volumes/postgres/data:/var/lib/postgresql/data
    ports:
      - '5432:5432'
